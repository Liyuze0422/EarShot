# 更新记录

本文件记录对外可感知的变化。版本号遵循「主版本.次版本.修订号」。

## 0.9.15

**双击就能用，不用先装 Python、不用敲命令。**
这一版把它打成一个独立程序：`dist/EarShot/EarShot.exe`（和同级的 `_internal/` 是一体的，要一起拷）。
图标是自绘的深色底 + 青蓝声波条（`tools/make_icon.py` 生成，随时可重跑），启动页右下角署名。

### 0. 怎么用

双击 `EarShot.exe`（仓库根也有一个快捷方式 `启动提词器（exe）.lnk`）→ 弹出「选择知识库」→
选一个、点右下角「开始 ▶」→ 窗口关掉、浮窗接管。和源码模式完全一样的流程，只是不需要环境。

### 1. 打包方式，以及三个取舍

- **选 onedir，不选 onefile**：onefile 每次启动都要把约 500MB 解压到临时目录，
  面试前等它解压没有意义。代价是分发时必须连 `_internal/` 一起拷，不能只拷 exe。
- **数据不进包**：`config/`、`knowledge/`、`logs/`、`知识库/` 全部留在 exe 旁边 ——
  准确说是「从 exe 往上找到有 `config/settings.json` 的那一层」。打进包里等于用户改不了、
  日志重启就丢。代码和数据的分家见 `server/settings.py` 的 `_split_roots()`。
- **一个 exe 分饰四个角色**：源码作为数据文件打进包，靠 `--role server|ui|library` 和
  `--run tools/xxx.py` 用 `runpy` 按路径跑。否则要打四个 exe，名字还难看。

### 2. 体积与启动时间（实测）

| | 初版 | 现在 |
|---|---|---|
| 包体 | 1063 MB | **486 MB** |
| 启动到浮窗可用 | — | 约 **40 秒**（源码模式 24 秒）|

省下来的 577MB 是排掉了 `torch` / `torchaudio` / `torchvision` / `pyarrow` / `matplotlib`：
项目代码里**没有任何 `import torch`**，ASR 走 onnxruntime，这些是被别的包的 import 链拖进来的。
`transformers` / `pandas` / `numba`（含 `llvmlite` 115MB）**保留** —— 不确定 funasr_onnx
是否真用到，宁大勿崩。想再压可以拿它们开刀，但必须用真实录音跑一遍再发。

启动多出来的 16 秒是 exe 首次读盘与校验，模型加载本身没变。

### 3. 打包踩的四个坑（都写进代码注释了，别再踩一遍）

1. **入口脚本不能同时出现在 `datas` 里** —— PyInstaller 会把它登记成 `DATA` 而不是 `PYSOURCE`，
   打出来的 exe 起来什么都不做：退出码 0、不写日志、不弹窗。
2. **`a.scripts` 每一项是 `(dest名, 源路径, 类型)`，dest 名不带 `.py`**。
   拿 `basename(s[0]) == 'launch.py'` 去挑永远是空列表，于是 EXE 拿到空入口 →
   生成一个没有入口的 exe，症状和 1 一模一样。spec 里现在有 `assert`，挑不到就让打包直接失败。
3. **`console=False` 打出来的 windowed 版根本起不来**：一启动就
   `Failed to start embedded python interpreter`（PyInstaller 6.22.3 实测）。
   改用 `console=True` + `hide_console='hide-early'`：同样看不到黑窗，且能正常启动。
4. **冻结进程起子进程，必须清 `PYTHONHOME` / `PYTHONPATH` / `_PYI_*`**：
   PyInstaller 给父进程设的 `PYTHONHOME` 会被子进程继承，子进程拿它去找标准库，
   而 PyInstaller 布局里没有 `lib/python3.12` → 解释器初始化失败，
   **子进程连一行输出都来不及写就死**。手动用 PowerShell 起同一个 exe 却完全正常，
   差异就在这几个环境变量上（见 `tools/launch.py` 的 `clean_env()`）。

**排查这类问题最省时间的一步：先打一个 `console=True` 的包。**
windowed 版会把异常塞进一个标题为 `Error` 的对话框里，看上去只是「双击没反应」——
实测就是这个框把真正的报错一直藏着。

### 4. 配套改动

- `tools/make_icon.py`：图标生成脚本（Pillow 绘 1024px 再缩小，输出 png + 多尺寸 ico）。
- `EarShot.spec`：打包配置，带注释解释每个决定的来由。
- `tools/launch.py`：新增 `--role` / `--run` 分发、`clean_env()`、frozen 下的路径推断；
  进程查找同时匹配 `EarShot.exe --role xxx`，所以 exe 版照样能用 `--stop` 收工。
- 打包产物（`dist/`、`build/`）不进仓库；`EarShot.spec` 进。

### 诚实边界

- **exe 版比源码模式慢 16 秒**，这是 onedir 首次读盘的固定成本，换不来。
- **没做代码签名**，首次运行 SmartScreen 可能提示「未知发布者」，点「仍要运行」即可。
- **只测过 Windows**。onedir 的产物可以直接拷到别的 Windows 机器（无需装 Python），
  但要在别的系统上跑得重新打包。
- **换库仍然要重启后端**（0.9.14 的行为），exe 版没有改变这一点。
- 回滚：删掉 `dist/` 即回到源码模式，`启动提词器.bat` 一直是可用的那条路。

## 0.9.14

**换一家面试要动 6 个地方、跑 3 个脚本、再重启；漏一步的症状是「答案里混进上一家公司的材料」。**
这一版把换库变成点一下的事。

### 0. 启动前多了一个「选择知识库」窗口

双击启动脚本不再直接起后端，而是先弹一个窗口：列出已有的库、每个库多少材料、题库备好没有。
选一个，点右下角「开始 ▶」，**这个窗口关掉、浮窗接管**。没有的库当场建：起个库名 → 选专属文档 →
选「其他 / 补充文档」（简历、项目报告这类所有场次都用的，进共用的 `_base/`）→ 顺手填一句公司背景。
`.md` / `.txt` 直接收，`.docx` **自动转成 markdown**（纯标准库抠 `word/document.xml`，不装 python-docx）。

两个容易被忽略、漏了就会出错的细节：

- **换库必须重启后端**：后端装的是旧库的索引和题库，复用它会照旧材料回答。
  窗口选了跟当前不一样的库时，启动器会先把装了旧库的后端停掉再起。
- **关掉窗口 = 取消启动**，不是「用默认库悄悄启动」—— 点了才起。

窗口置顶显示：不置顶时它会被别的窗口压在后面，看起来像「双击了什么都没发生」。
命令行 / 自动化走 `python tools/launch.py --no-library` 跳过它（`tools/switch.py` 就是这么调的）。
体检里加了「选库窗口 + 截图」一项（现在 12 项，`--fast` 8.4 秒）。

### 1. 题库按资料包分文件

`知识库/题库.json` 一直是单份的，换包就得整份重建（约 1 分钟）。现在每个包各留一份
`知识库/题库_<包名>.json`：`server/bank.py` 新增 `bank_path()`，优先用当前包的那份，
没有才回退到通用的一份。**跑过的包切回来直接复用。**

配套改了四处：`tools/build_bank.py` 的输出跟着当前包走；`tools/preflight.py` 的题库新鲜度
检查改用同一个入口（不再各抄一份路径）；`server/knowledge.py` 的 `set_profile()` 补上清题库缓存
（不清的话切完包拿的还是上一个包的题库）；隐私门禁补了一条探针，确认新文件同样被 `.gitignore` 挡住。

### 2. 新增 `tools/switch.py`：一条命令切包

```bash
python tools/switch.py --list        # 有哪些包、当前用哪个
python tools/switch.py <包名>         # 一键切（配置 + 题库 + 扩展词 + 自检 + 重启）
```

里面有两步是踩坑换来的：

- **串场扫描**：在 `_base/` 里找**其它资料包**的痕迹。实测切到新包之后，基础材料里
  还留着上一家的整段话术（薪资区间、城市、创始人称呼），它们照样被检索命中、照样被照念 ——
  **新包本身是干净的，问题全在旧残留**，而这一步以前全靠人记得去查。
- **公司画像遮蔽检查**：`config/company.md` 的优先级高于包里的 `company.md`
  （见 `answer._company_path`）。**它存在一天，换包就换不掉公司介绍。**
  加 `--take-company` 让它让位给包里那份。

题库重建默认走 `--merge` 而不是 `--force`：覆盖式重建会让题库变小（实测 502 → 487），
而题库一变小，追问预案就哑了（它要求同一章节命中 ≥2 条），症状看着像代码坏了。

### 3. 基础材料里的跨场残留清理

`knowledge/_base/` 里几份**绑定上一家公司**的材料改成了通用版：写死的薪资区间删掉、
反问清单去掉城市、HR 题库去掉创始人称呼、岗位匹配表改成下划线开头（不参与检索，
需要时去掉下划线即可恢复）。**以后再换场，用 `switch.py <包> --check` 看串场扫描是不是「✓ 干净」。**

## 0.9.13

**浮窗的三个实际使用问题：缩不回去、调不了大小、看不了上一题。**

### 1. 「缩略」按下去就找不到了

状态栏那个 `—` 按钮调的是 `showMinimized()`。但浮窗是 `Tool` 型的**无边框窗口** ——
它不进任务栏，最小化之后屏幕上不留任何可点的地方，**只能靠记住 `Ctrl+Shift+H` 才能找回来**。

**改法**：

- `—` 变成「**缩略成小条**」：整窗压成一行状态栏（实测 **491px → 60px**），**贴在原处不消失**
- **点这一条任意位置就还原**（鼠标移上去变手型）
- 另加**系统托盘图标**：双击显示 / 隐藏，右键菜单有显示、缩略、暂停、恢复默认大小、退出

### 2. 框的大小调不了

窗口高度本来按内容自适应（折叠 150~420、展开 320~900），**宽度写死 580**，
而且**没有任何拖拽把手** —— 无边框窗口没有系统边框，所以只能干看着。

**改法**：

- 右下角加 `QSizeGrip` 拖拽把手
- **拖过一次后进入「用户说了算」模式**：自适应只在内容塞不下时长高，**不再覆盖你调好的尺寸**
- 尺寸存 `config/ui_state.json`（已 gitignore），下次启动自动恢复
- 托盘菜单里有「恢复默认大小」

### 3. 想看上一题

**改法**：新增 `Ctrl+Shift+← / →` 翻历史（每题答完自动存档，保留最近 40 条）

- 翻的时候问题、核心句、展开区**一起回到那一条**，顶部显示 `【历史 2/3 · 14:32:10】`
- 状态栏提示 `历史 2/3`；**翻到最新自动回到当前这一题**，不用手动退
- **新答案一到自动回到最新** —— 不会卡在历史里错过当前问题

### 实测（`ui/app.py --selftest`，新增三个场景）

| 检查 | 结果 |
|---|---|
| 缩略成小条 | **491px → 60px，仍可见** PASS |
| 点小条还原 | PASS |
| `Ctrl+Shift+←` 翻两条再翻回最新 | **位置 1 → 0 → -1，核心句逐条正确** PASS |
| 拖到 700x520 后自适应不再覆盖 | PASS |
| 托盘图标 | 已创建 |
| 两个新热键 | **系统级注册成功**（不是退化的窗口内快捷键） |
| 原有 4 个热键 / 展开 / 深答 / gap 标签 / 连问防串题 | **全部未受影响** |

### 诚实边界

- **托盘图标是程序画出来的**（蓝底白点），不是设计过的图标。
- **缩略小条高度是算出来的**（状态栏那行 +18px），换字体或改字号可能差几像素。
- **历史只在内存里**：浮窗重启就清空，没有落盘。面试一场内够用；跨场要翻 `logs/session_*.jsonl`。
- **翻历史时不暂停识别**：这时来新问题会自动跳回最新。这是有意的（别错过当前问题），
  但也意味着**你看历史看到一半，新答案可能把画面抢回去**。

### 可回滚

```powershell
git checkout -- ui/app.py .gitignore
```
## 0.9.12

**修一个「每个新用户都会看到」的误报：模型下完了却说还缺文件。**

### 怎么发现的

为回答「别人照教程能不能搭成我这样」，在一个全新目录里完整走了一遍一键安装
（`scripts/setup.ps1`），最后一步下载模型时打出：

```
目录内容:
        0.0 MB  am.mvn
        0.4 MB  chn_jpn_yue_eng_ko_spectok.bpe.model
        0.0 MB  config.yaml
        0.0 MB  configuration.json
      230.0 MB  model_quant.onnx
        0.3 MB  tokens.json

[!] 还缺 1 个文件: configuration.json
```

**退出码 1** —— 而那个文件明明就在，56 字节，内容也对。

### 根因

`tools/download_model.py` 收尾核对用的是**一个写死的统一阈值**：

```python
missing = [f for f in need if not os.path.exists(...) or os.path.getsize(...) < 1000]
```

而 `configuration.json` **本身就只有 56 字节**（`{"framework":"Pytorch","task":"auto-speech-recognition"}`）——
**永远小于 1000，永远被判「缺」**。

### 影响

**每一个下载成功的用户都会看到这条「还缺文件」、并拿到退出码 1**，以为是自己装坏了；
`setup.ps1` 也会把这次安装记成失败。而 `model_quant.onnx` 那 230MB 是好的。

### 修法

改成**按文件各自的实测大小设阈值**（统一阈值对小文件天然不适用）：

| 文件 | 实测大小 | 现在的阈值 |
|---|---|---|
| `model_quant.onnx` | 230 MB | 100 MB |
| `tokens.json` | 352 KB | 100 KB |
| `chn_jpn_yue_eng_ko_spectok.bpe.model` | 377 KB | 100 KB |
| `am.mvn` | 11 KB | 4 KB |
| `config.yaml` | 1855 B | 500 B |
| `configuration.json` | 56 B | 10 B |

阈值按实测值打对折：既能挡住「下到一半断了」，又不会误报。

### 验证

| 检查 | 修前 | 修后 |
|---|---|---|
| `download_model.py` 收尾输出 | `[!] 还缺 1 个文件` / 退出码 1 | **`6 个文件齐全` / 退出码 0** |
| 全新环境完整自检（带模型，32.1s） | 模型项 FAIL | **PASS 7 / FAIL 1**（唯一 FAIL 是没填密钥，正常） |

新增 `tests/test_download_model.py`：断言每个阈值都小于真实文件大小、
最小的那个文件绝不能用 KB 级阈值判。

### 诚实边界

- 阈值是**实测值打对折**，不是官方规格。ModelScope 上模型版本更新后文件大小若变了，
  阈值该跟着调 —— 测试里那张 `REAL` 表就是基准。
- 全量自检 32.1 秒（含 ASR 加载 + 首次识别 + 浮窗启动），比 `--fast` 的 6 秒慢；
  面试前 30 分钟那次用 `--fast` 就够。

### 可回滚

```powershell
git checkout -- tools/download_model.py tests/test_download_model.py
```
## 0.9.11

**修一个自己写文档踩出来的坑：说明文字被当成词表读进去了。**

### 怎么发现的

为了回答「别人 clone 下来怎么装」，真的在一个新目录里克隆了一份、按 README 走了一遍。
跑 `tools/preflight.py`（自检）时，第 5 项「专名词表体检」报出来：

```
5  专名词表体检   WARN  0.05s  会 0 个 · 没做过 58 个
                               never_used 里疑似混进说明文字: gap（我会的；
                               never_used 里疑似混进说明文字: 不是专名）」；
                               never_used 里疑似混进说明文字: 不清楚"的方向。那是真正该填的。；
```

0.9.10 那一节往 `config/never_used.example.md` 里写了一大段「怎么挑词」的教程，
**那些说明句子被 `_load_list()` 拆成了 58 个词条**。

### 后果有多严重

`never_used` 里的词**命中即判缺口题**。那 58 个垃圾词里有一个是 **`python`** ——
**任何人问「python 你熟吗」都会被判成「我没做过 python」**。

（本机的 `config/never_used.md` 只有 6 行干净词条，**没受影响**；受影响的是
clone 仓库后自己没有那份、退回读 `.example` 模板的人 —— 也就是**所有新用户**。）

### 修法

| 层 | 改动 |
|---|---|
| `_load_list()` | **成对反引号围起来的代码块整段跳过** —— 示例命令里的 `python tools\...` 就是这么进来的 |
| `_load_list()` | 单个词条里出现中文句读/括号引号（`。；：？！「」《》…`），或者长度超过 16 —— 那是在写句子，不是在写词表 |
| `never_used.example.md` | 说明行全部改成 `>` 引用块；并且把「哪些写法不会被当成词条」写进模板开头，免得下一个人再踩 |

### 验证

| 检查 | 修前 | 修后 |
|---|---|---|
| 新用户环境（clone 后跑 preflight）「没做过」 | **58 个** | **0 个** |
| 本机 `known_terms` 白名单 | 114 | **114**（没被误伤） |
| 本机 `never_used` 条目 | 6 | **6** |
| `tools/test_gap_fp.py` | 误报 0 | **误报 0** |
| `tools/regress.py` | 全过 | **全过** |

新增两个测试：`test_load_list_ignores_prose_and_code_blocks`、
`test_shipped_example_file_is_clean`（后者直接断言仓库里那份模板解析出来是空集）。

### 诚实边界

- 过滤挡的是「**像句子的东西**」，不是「所有不该进词表的东西」。真在词表里写一整句
  （比如「我们用的就是 kubernetes」）、又没超过 16 字、又没有中文标点，还是会被吃进去 ——
  模板开头已经写明「一行一个词」。
- 过滤器只看单个词条，**不看你写在哪**：如果将来的说明文字恰好全是短英文词
  （「use it only here」），还是会被当成词条。`>` 开头是唯一稳妥的写法。

### 可回滚

```powershell
git checkout -- server/answer.py config/never_used.example.md tests/test_gap_prompt.py
```
## 0.9.10

**缺口题不再答"我不知道" —— 改成"没落到项目里，但我了解、我对比过、我知道什么时候该加"。**

### 用户的原话

> "每次面试官一问道多模态相关的知识问题，就回复了不知道不清楚，我感觉这样也是不对的。
> 应该是：我没有实际跑到项目中，但是我了解，我知道，而且进行了对比，说出我现在简历
> 这样做的优势是什么，这个技术是什么，以及在什么条件下可以进行扩充加入。"

### 旧答法错在哪

`S_GAP` 第一段写的是"**直接承认没接触过**……例：这块我确实没有实际用过，不装懂" ——
**开口就是认输**。而且四段是【坦诚】【我的理解】【迁移与路径】【反问】，
既没有"我现在的做法优势在哪"，也没有"什么条件下该加进来"。

改动前实测（`python answer.py "CLIP 你了解吗？"`）：

```
【坦诚】CLIP 我确实没实际用过，不装懂。
【我的理解】…但这是我按原理推的，不是我的经历。
【迁移与路径】我做过检索排序和 RAG…
【反问】…
```

### 改法

四段重排，并把认输话术列入禁用：

| | 旧 | 新 |
|---|---|---|
| 第一段 | 【坦诚】直接承认没接触过 | 【实话】一句带过"没落到项目里"，**同一段内立刻转到原理** |
| 第二段 | 【我的理解】原理 | 【对比】**我这条线用的是 XX、当时的约束是…；我这个场景为什么现在不需要它** |
| 第三段 | 【迁移与路径】哪块经验能接上 | 【什么时候用】**什么条件下值得把它加进来** |
| 禁用 | —— | **不许写"我不懂/我不会/不清楚/没接触过/不了解/不装懂"** |

另外给 gap 题加了一次**固定锚点检索**（`GAP_ANCHOR_Q`），把材料里本来就写好的
"缺口题怎么答 / 技术选型怎么讲取舍"固定召回进上下文（实测换过十几条问法，
只有这条能稳定把这两类材料排进前 3）。

### 改动后实测（`answer.py` 直接跑，不动音频）

```
Q: CLIP 你了解吗？
【实话】没落到项目里，但原理我清楚——CLIP 靠对比学习把图像和文本映射到同一空间，
        主要解决零样本分类和跨模态检索。
【对比】我这条线上用的是 TF-IDF 加向量的混合检索，约束是纯文本法规库、离线环境；
        输入里没有图像，所以用不上它。
【什么时候用】一旦要跨模态检索、或者零样本分类，才值得加进来。
【反问】你说的 CLIP 是指这个图文对比模型吧？
```

| 问题 | 【对比】段实测输出 |
|---|---|
| 你用过 Qwen-VL 吗 | 我这条线是多机协同和任务规划，输入基本是结构化指令和遥测数据，用纯文本模型就够了，多模态在这个场景收益不高 |
| ClickHouse 你用过吗 | 我这条线上做检索用的是向量加关键词双通道，数据量在百万级以内，MySQL 加向量库就够，没到需要列存分析引擎的规模 |

### 补充：判据那半边本来就有开关，只是清单是空的

上面第 1 条边界（"材料里出现过、但我没做过"的词走不到缺口模板）**不用改代码就能解**：

`server/answer.py` 里早就有 `never_hit()`，它读 `config/never_used.md` ——
**"材料里出现过、但我其实没做过"的人工清单**，命中就判 gap。文件里连用法都写好了，
但清单一直是空的（只有注释和示例）。

填了 6 条多模态/视觉语言方向的词：`多模态` `视觉语言模型` `VLM` `CLIP` `Qwen-VL` `图像理解`。

**填之前先验证了"不影响判据"**：

| 检查 | 结果 |
|---|---|
| 命中 tests/real_questions.json 的 50 条真实问题 | **0 条** |
| tools/test_gap_fp.py | 误报 **0**，硬案例 **10/10** |
| tools/regress.py（9 项） | 全过，100% 不变 |
| "我会的/不是专名"那 12 条 | 全部仍然**不判** gap |

实测 `你了解多模态大模型吗？`：

```
【实话】没落到项目里，但原理我清楚——多模态大模型是把文本、图像、语音映射到同一表征空间…
【对比】我这条线是纯文本的 RAG 双通道检索加 Agent 编排，输入就是文字，没有图像语音的模态需求。
【什么时候用】等业务真出现图文混合输入、且标注和算力跟得上时，才值得引入。
【反问】你说的多模态，是指图文理解，还是也包括语音这块？
```

`SLAM` **故意没填** —— `tools/test_gap_fp.py` 把"ROS2 和 SLAM 你熟吗"列在"我会的"那一节，
而且无人机项目材料里有相关描述，默认不判缺口（文件里留了取消注释的说明）。

### 补充二：【展开】段（用户点名要保留的写法）

用户说"展开这个我个人很喜欢，可以保留"。经历/行为/动机/通用四个模板的第二段一直是
`【展开】`，缺口题没有 —— 于是给它补上，五段变成
**【实话】【对比】【展开】【什么时候用】【反问】**。

难点在约束：【展开】是缺口题里**唯一允许讲细节**的地方，但缺口题的前提是"我没做过"。
一旦允许展开面试官问的那项技术，模型就会往编的方向使劲。所以写死了
"**不许展开面试官问的那项技术**：不写它的架构分层、参数、训练细节、部署方式，
更不许编'我踩过的坑'"—— 展开的必须是**我自己那条线**。

实测（`python server/answer.py`，四条）：

| 问题 | 【展开】段输出 |
|---|---|
| CLIP 你了解吗 | 检索层我做的是 TF-IDF 加向量混合，Top-K 命中率 29 组用例、89.7% Top-1。 |
| 你了解多模态大模型吗 | 我那套是 TF-IDF 加向量混合检索，双通道召回后做忠实度校验，29 组用例 Top-1 命中 89.7%。 |
| 你用过 Qwen-VL 吗 | 我那套是向量加关键词两路召回，断网也能查，工具调用外面套了安全护栏。 |
| eBPF 你做过吗 | 我的活是任务规划、多机协同和知识库检索，接口和部署层面的事。 |

**四条都没有展开那项技术本身**，数字全部来自材料。字数 198~248 字（原 179~193），
首字 1.3~1.6 秒没变差。新增测试 `test_gap_expand_section_only_expands_my_own_work`。

### 诚实边界（续）

1. **只有走 gap 策略的题才用新模板** —— 判据是"材料词表里有没有这个词" ∨ "never_used 清单里有没有"。
   后者靠人工维护，**清单没填的方向仍然走不到**（上面这 6 条是用户明确反馈的方向，
   别的方向要加就照 `config/never_used.md` 里的说明加）。
2. 【对比】段里的"我这条线用的是 XX"只能引材料里写过的做法，prompt 里写死了这条；
   实测四次都没编造，但这是**概率性的，不是硬保证**。
3. 代价：gap 题首字从 1.2~1.4 秒涨到 1.4~1.6 秒（多了一次本地检索）。

### 可回滚

```powershell
git checkout -- server/answer.py tests/test_gap_prompt.py
```

## 0.9.9

**场景题说到一半就被当成完整问题答掉 —— 判据从"看长度"改成"只看尾巴"。**

### 问题

实机演练里用户看到：HR 出一道场景题 —— "我们脚本给到你之前是一个 A 的版本，
然后你在工作过程中先按照脚本 A 去写……然后就做算法调度这些嘛"，停一下再补
"但是你的代码已经写死了，你会怎么去解决这个问题呢"。

前半段够长（> 40 字），旧判据第一行 `if len(t) >= 40: return False` 直接放它过去，
**立刻答掉**；真正的问法来的时候主题已经被占了。复盘日志里这条是
`[A 11:39:26]`，答的是"脚本版本迭代这块，我做的是先对齐需求再动手" —— 对着前半段答的。

### 改法

`_is_half_sentence` 的判据改成只看尾巴：

| | 旧 | 新 |
|---|---|---|
| 长度 ≥ 40 字 | **直接当说完** | 不再看长度 |
| 疑问信号 | 整句搜 | 只看**最后 15 个字** |
| 句末标点 | 有标点就当说完 | 剥掉标点后再看结尾 |
| 结尾 | 短于 12 字或挂在连词上 | 短于 12 字或挂在连词/助词/**语气词**上 |

判成半句 → 进 hold，最多再等 4 秒（改动前是 0.6 秒就发），面试官接着说就拼起来一起送。

### 影响面（拿这次演练的 42 条真实转写做 A/B）

14 条从"立即答"变成"先等几秒"。逐条看过：**约一半确实是半句**
（"我的自我介绍完毕，谢谢。我想了解一下，就是您在项目中你是有。"、
场景题前半段、"……就现在我们目前的任务流程中也是有"），
另一半是"以语气词收尾的完整问题"（"……你有过机器人的实操经验嘛。"）—— 代价是多等几秒。

### 实机复验（同一段音频重播，改动前后各一次）

| 片段 | 改动前 | 改动后 |
|---|---|---|
| HR 开场提问（源 01:10-01:55） | 答了两次：先答半句，7 秒后再答完整 | **一次答对**，首字 235 ms |
| 场景题（源 18:10-19:15） | 前半段被单独答掉 | **完整合并**，首字 777 ms |

### 诚实边界

1. **只覆盖 0.6~4 秒的停顿**。HR 停 7 秒、15 秒以上的，合并窗口够不到，
   仍然会先答前半段。
2. **代价是延迟**：以语气词/连词收尾的完整问题会多等最多 4 秒。
   嫌慢就把 `config/settings.json` 的 `hold_incomplete` 改成 false（回到旧行为）。
3. 判据仍然是**纯文本启发式**，没有声学信息。"他说完了没有"本质上只有静音时长能判，
   而静音时长已知不够用 —— 面试官会想很久。

## 0.9.8

**第一次实机验收：把真实面试录音从播放设备播出去，走完整采集链路。**
抓到一个会漏放真问题的 bug，已修，代价为零。

### 为什么之前的数字都不算数

0.9.7 之前所有指标都是**回放** —— 把问题文本直接喂给后端，跳过采集端。
采集端的风险（VAD 什么时候切句、面试官说到一半停顿会怎样、ASR 丢字）
回放测不出来。

而 EarShot 只听 WASAPI 回环（= 系统正在播放的声音），所以**文本形式的模拟
面试官一个音都进不来**。要验实机只有一条路：**把真实录音播出来**。

做法：`_realdata/` 里的真实面试录音 → 默认播放设备 → EarShot 回环采集 →
完整链路 → 读 `logs/session_*.jsonl` 复盘。

### 实测（两场真实面试录音，共 46 分钟）

| | 技术一面（播 18 分钟） | HR 面（播 28 分钟） |
|---|---|---|
| 片段里的真问题 | 13 条 | 18 条 |
| 答出 | 11 | 14（人工复核后 16） |
| 采集中断 | 0 | 0 |
| 首字中位 | 711ms | 645ms |
| 总中位 | 1775ms | 1568ms |
| 最慢 | 2420ms | **14401ms** |

延迟和回放口径一致（回放中位 1.58s）—— 采集端没让端到端变慢。

### 抓到的 bug：`r'麦克风'` 一票否决

`server/router.py` 的 SKIP 列表里有两个裸设备词：

```python
r'声音(大|小|放大|清楚)', r'麦克风', r'摄像头', r'视频(出来|开了?|卡|没)',
```

HR 问"那如果从麦克风开始的话到机器人做出动作，你们中间会经过哪一些环节呢"，
整句**只因为含"麦克风"三个字就被判成设备调试话**，一道核心问题直接漏放，
屏幕上什么都没出。这条是用户盯着浮窗亲眼看到的。

修法：删掉两个裸设备词。设备调试话本来就不带疑问形式，末尾的
`QUESTION_FORM` 兜底拦得住（"稍等我看一下麦克风" 无问句 → skip）。
**代价实测为零**：

| | 改前 | 改后 |
|---|---|---|
| 非面试音频误放行 | 14.1% | **14.1%** |
| 真提问召回 | 100% | **100%** |
| 面试官闲聊/设备拦截 | 6/6 | **6/6** |

回归门禁、76 条单测、ruff 全绿。新增防回归测试
`test_device_words_do_not_kill_a_real_question`。

### 诚实边界（这段别跳过）

1. **这是混轨录音**：面试官和候选人都在同一条音轨里，都被回环采到。
   真实面试里回环**只收面试官**（候选人说话走麦克风）。后果：
   误放行被系统性放大（屏幕上多出来的答案大多是"候选人自己说的话"）；
   面试官的话被候选人插话打断 → ASR 丢字 → 漏放被放大。
   两场共 6 条漏放，根因都是这个，不是检索或路由。
2. **说话人分离不可行**：实测左右声道内容一致（整段波形相关 0.75，但分声道
   转写出的文本相同），不能靠声道拆人。
3. **HR 面 14 条"多出来的答案"里约 10 条是候选人自己的话**，真实场景不发生。
4. **一条 14.4 秒的慢尾**（首字 12.7 秒），只出现过一次，原因未定位。
5. **合并窗口的取舍**：把相邻两句黏成一次识别，面试官连说两句时是对的
   （一起答）；但前面是别的话题时，答案会偏向前面那段。这次**没改** ——
   改生成提示词影响面太大，验证不充分不敢上。

### 可回滚

```powershell
git checkout -- server/router.py tests/test_router_gate.py
```

## 0.9.7

**修的不是检索，是"该不该答"这道闸。**

### 为什么去查这个

用户问："真正的瓶颈是什么？这东西是要在面试里用的，不能测试时匹配率高、
实战里一个问题都答不出来。"

于是不再看基准，去读**三场真实会话**的原始日志（955 / 96 / 24 个事件）：

| 会话 | 答了几条 | 真正是"问你的问题"的 |
|---|---|---|
| 英雄联盟解说（放在那没关） | 225 | **0** |
| B站视频（鱼油广告/政论/面试技巧） | 17 | **2** |
| 真实面试 | 10 | **3** |

**252 条答案，约 5 条有用。**

### 端到端先验一遍：核心管线是好的

在唯一有人工标注的那场真实面试上，把 50 条题**走完整链路**回放：

- 44 条真问题 **全部答出，0 条判"没接触过"**，命中金标 39/44
- 中位 **1.58 秒**（首字 0.69 秒），最慢 2.3 秒
- 逐条读过答案：具体、第一人称、带数字。那条"是三维还是只在渲染"的回答
  直接纠正了面试官的理解，并写明"我不声称完整 3D 避障"

**所以检索和生成不是瓶颈。** 瓶颈在"判断这句话该不该答"。

### 根因：把"像不像问句"当成了"是不是在问我"

`server/router.py` 只有 87 行，最后一条兜底是：

```python
QUESTION_FORM = re.compile(r'[?？]|吗|嘛|呢|什么|怎么|咋|为什么|为啥|哪|多少|...')
```

**只要出现"什么/怎么/为什么"就当在问我。** LOL 那些被答的句子正是这样进来的：
"他**怎么**先落位的"、"**什么**都没交啊"、"**为什么**这么少呢"。

实测这条兜底一个人贡献了 **95% 的误放行**，同时扛着 **89% 的正确召回** ——
所以不能删，只能收紧。

### 改法

收紧方向是「**问的形式**」而不是「有疑问词」：疑问词要挨着动词，或者前面有"你"。

| | 误放行（953 条环境音）| 召回（44 条真问题）|
|---|---|---|
| 改前 | 25.4% | 100% |
| **改后** | **14.1%** | **100%** |

**误放行砍掉 44%，召回一条不掉。**

顺带修了一个**本来就存在、被松兜底掩盖了**的结构问题：`is_skip` 只看兜底、
不看 DESIGN/BEHAVIOR 那几条明确规则，导致"说一次你做得比较失败的项目"这种
不含疑问词的行为题会被判成废话。兜底和明确规则是**或**的关系，不是先后关系。
修完合成路由准确率从 91.7% 回到 **100%**。

### 诚实边界

- **14.1% 还是高。** 这只是把误放行砍了一半，不是解决。剩下的主要来自
  环境音里确实带疑问结构的句子（"怎么才能…"）。
- **我没有说话人分离。** 工具不知道是谁在说话。日志里能看到它把候选人自己的
  自我介绍当成问题答了一遍 —— 但那几场是**录音回放**（loopback 采集不会听到
  你自己的麦克风），实战里大概率不会发生。**这条我没有实机验证过，别当结论。**
- 真实面试那场只有 10 条答案，样本极小。

---

## 0.9.6

**面试官不会照抄你材料里的说法 —— 这是检索失败的最大单一来源。**

### 问题量化

你材料写「LangGraph 九节点有向图工作流」，面试官问的可能是「你那套任务编排
是怎么设计的」；你写「自然语言解析三级降级链」，他问「你说的那套语义理解
具体指什么」。**一个字都不重合**，词级 BM25 就无从下手。

造了 19 条"换说法"基准（拿真实题号，把材料的专有措辞换成通用概念词）实测：

| 面试官的说法 | hit@1 | hit@3 |
|---|---|---|
| 照抄材料措辞 | 61.5% | 74.4% |
| **换成他自己的通用说法** | **15.8%** | **42.1%** |

**但要看清楚场景**：这是**没有上下文**时的数字。把面试官前几句原话作为历史
（就是真实面试的样子）再测，换说法的 hit@1 是 **78.9%** —— 话题栈已经在兜底。
所以真正的暴露面是**冷启动的第一次提问**：换了个说法、之前没聊过这块。

### 做法：离线把面试官的说法预先编进索引

新增 `tools/kb_expand.py`（doc2query）：逐块读你的材料，让模型生成 5 个
「面试官可能怎么问这块」的通用问法，编进这一块的检索词袋。
**运行时不发任何请求**，延迟和花费都不变。产物 `config/doc_expand.json`，
按**内容 hash** 索引 —— 材料改一个字这块就自动失效、退回纯原文检索，
不会张冠李戴。

### 实测（338/338 块全覆盖）

| | ① 原题(39) | ② 换说法无历史(19) | ③ 换说法带历史(19) |
|---|---|---|---|
| 关闭 | 89.7 / 94.9 | 15.8 / 42.1 | 78.9 / 84.2 |
| 开启 | 89.7 / 94.9 | **26.3 / 47.4** | 78.9 / 78.9 |
| 变化 | 0 / **0** | **+10.5 / +5.3** | 0 / **−5.3** |

**诚实边界**：
- 目标场景 hit@1 从 15.8% → 26.3%（**1.67 倍**），hit@3 42.1% → 47.4%。有效果，但没到"解决"。
- ③ 的 hit@3 **掉了 1 条**（19 条里的 1 条）：扩展词把一份**无关材料**顶进了
  top-3，把正确的那份挤了出去。这是扩展词"过度加成"的典型副作用。
- **n=19 太粗，1 条就是 5.3%**，这两个数字都在噪声量级上，方向可信、幅度别当真。

### 中途试错（记下来省得重走）

1. **把扩展词混进词袋、并且算进 `dl`** → 原题 hit@3 掉 2.6（词袋变大，
   长度归一化压低了该块所有真实词的分数）。
2. **改成独立 RRF 通道** → **更差**：单元测试 70.5% vs 词袋法 100%。
   RRF 是排名制，一个通道排第 1 只给 `weight/(60+1)`，量级比不过直接进 TF-IDF。
   而且权重从 0.2 扫到 1.0，基准上**一行都没变**。
3. **最终方案**：扩展词进 **TF**，但 **`dl` 只算原文**。
   这条同时保住了增益（② hit@1 +10.5）和主门禁（① 一点没动）。
   代价是增益比第 1 版小（+15.8 → +10.5），换来的是一条真实题不再退化。

### 顺带

- `tools/preflight.py` 新增「换说法扩展词」检查：覆盖率不满就提示重跑
  `kb_expand.py`。这类退化很隐蔽 —— 检索照常工作，只是换个说法就问不出来。
- `config/doc_expand.json` 由你的私有材料生成，**已 gitignore**。

---

## 0.9.5

**ASR 结巴造出的假名词，会让工具把会的题答成"我没接触过"。**

### 现象

用户反馈"漏字"：面试官问「我看你的简历里写了自然语言调度，这个调度具体是怎么
实现的？」，工具回答成——

> 【坦诚】调度具这个词我确实没接触过，不装懂，我材料里也没有对应的东西。

**而检索其实命中了正确材料 5 条。** 用户看到的就是"没识别到、直接不给答案"。

### 根因（转写其实是对的）

会话日志里的原文是：

> 嗯，描述就用了自然语言调度这个说法，那我麻烦你讲一下调度**具具体**指的是一层？

ASR 把「调度**具体**」转成了「调度**具具体**」（重复了一个"具"），然后：

1. `jieba` 把它切成 `调度(n) + 具具(v)` —— **"具具"是个 jieba 不认识的叠字**
2. `unknown_cjk` 的判据是"成分里至少有一个连 jieba 都不认识的词 = 生造词"，
   于是把 **「调度具具」** 当成了面试官嘴里的技术名词
3. `is_unknown_gap` 判 gap → 走"坦诚"策略 → 回答"我没接触过"

### 修复

在 `unknown_cjk` 里加一条**很窄**的规则：不认识的成分如果**全是叠字**，那是转写
结巴造成的，不算生造词。窄是故意的 —— `湖仓一体`/`存算分离`/`具身智能` 里的
`湖仓`/`存算`/`具身` 都不是叠字，不受影响。

### 试过并否掉的方案（记下来省得重走）

先加的是"检索覆盖率够高就不判 gap"的守卫。**方向是错的**：覆盖率算的是
"问题的内容词在材料里出现多少"，而真正该报 gap 的题（eBPF / ClickHouse /
湖仓一体）覆盖率本来就低；反过来这次误判的题覆盖率只有 **0.25**，根本拦不住。
结果是该报的被压掉、该拦的没拦住，`test_gap_fp` 从 **10/10 掉到 6/10**。
已删除，只留上面那条窄规则。

### 回归

`test_gap_fp` 10/10（误报 0）、`tools/regress.py` 全过、pytest 64 → **67**。

---

## 0.9.4

**四个真实使用问题的系统性修复。** 全部先量再改，负面结果一并记下来。

### 一个比反馈本身更严重的问题：`extract_q` 从未生效

用户反馈"语义匹配不准、无法正确理解面试官发言"。查下来根因不在匹配算法：

**真实面试录音 150 秒，ASR 产出 0 个标点。** 而 `extract_q.extract()` 的整句切分
就是靠标点（`[。！？!?；;…]`）—— 没标点就永远只有一段，于是那个 200 多行的
抽取模块在生产里等于没装：面试官 100+ 字的铺垫+自述+提问**整坨**进了 router 和检索。

在真实会话日志里验证：**953 段转写，含标点的 0 段（0.0%）**。

修复：`transcribe` 的 `textnorm` 从默认 `woitn` 改成 `withitn`。

| 真实录音 150 秒 | woitn（原） | withitn（新） |
|---|---|---|
| 标点数 | **0** | **46**（句末 20） |
| `extract_q` 切出几段 | **1** | **18~20** |

回退：`config/settings.json` 里设 `"asr_textnorm": "woitn"`，不用改代码。

### 1. 面试官停顿一下再说话，答案就变了

**根因**：VAD 静音 600ms 就判"说完了"→ 立刻发答案；第二段来了再发一次，
新 qid 把上一个**顶掉**。全程没有任何合并窗口。

**修复**：新增 `MergeGate`——判停后先攒着，窗口内只要又出声就取消待发，
等它说完拼成一句再送；另加半句判定（以连接词收尾、或既无疑问标志又很短就先存着）。
抽成纯逻辑类是为了能单测（采集循环要真实音频设备，测不了；**写测试当场抓到一个真 bug**：
取消待发时把 deadline 设成 None，正好落进"立即发送"分支）。

新配置：`vad_merge_ms=600` / `hold_incomplete=true` / `max_hold_s=4.0`，设 0 可关。

### 2. 英文技术词识别不出来（React 等）

**实测**：35 个真实术语，中文口音读中英混句只有 **7/35 = 20.0%** 原样识别，
英文口音也只有 14/35 = 40.0%。典型错误：`Redis→read`、`Nginx→links`、
`Kubernetes→kubbernet`、`Milvus→mivis`、`FastAPI→fast api`、`ONNX→o n n x`。

**修复**：新增 `server/termfix.py` —— 拿用户自己的 `known_terms.md` + 语料英文词
做保守模糊纠错（含相邻 token 合并，修 `fast api`→`FastAPI`）。

**分级信任**是核心：`known_terms`（用户手写，114 个）允许完整编辑距离；
语料词表（7000+ 词，混着 read/phase/links 这类普通英文词）**只允许距离 ≤1**。
第一版没分级，实测出现假阳性 `GraphqL`（本来就对）→ `graphpy`。

| 生产路径（35 术语 × 2 臂） | 识别率 |
|---|---|
| 纠错前 | 25/70 = 35.7% |
| 纠错后 | **38/70 = 54.3%** |
| 假阳性 | **0 条** |

### 3. 知识库整理没有统一规范

新增 `docs/知识库整理清单.md` + `tools/kb_audit.py` + `tools/kb_prompt.py`。

规范不是风格偏好，每条都对应代码里的一个机制：文件名在词袋里占 **4 倍权重**、
按 `##` 切块且 **60~700 字**（超了硬切、短了丢弃）、每块要自带项目名、
`topic_terms.json` 的键必须和文件名片段一致。

**为什么这件事最值钱**：检索最大的杀手是**同题材竞争**——掺 25% 「讲同一批话题」
的材料，hit@1 从 89.7% 掉到 **41.0%**；而掺 2 倍异题材文档只掉 2.5。

`kb_audit` 在真实知识库上跑出 7 项待改进，其中一条是真问题：**两份不同项目的报告
词汇重合度 57%** —— 这正是上面那个抢排名机制的直接来源。这类问题肉眼读材料看不出来，
只有把每块切成词集两两比对才会暴露。

### 4. 跨轮指代（面试官不再重复那个名词）

把单锚点升级成**话题栈**：保留最近 3 个「在聊的东西」，由近及远分别融合、
越远权重越低（0.6 衰减），超过 TTL 轮没再命中就出栈；HR 题仍不向后传递。

**诚实边界（重要）**：在同一份基准上 A/B，单锚点 vs 全栈 ——
**44/44 逐题 top1 完全相同**，hit@1 89.7% / hit@3 94.9% / 并集 97.4% 一模一样。
原因是真实面试官会连着十几轮聊同一个项目，多出来的锚点基本指向同一个文件。
这个栈只在「聊完 A、插两句别的、再回 A」时才有用，而那份基准是连续问答。
所以：机制有 10 条单测，**端到端收益未经验证，也未见伤害**。

### 顺带修掉

`answer._VOCAB` / `answer._FUZZ` 是模块级 memo，切资料包时从不清 ——
会拿着上个资料包的词表做术语纠错和缺口判定。现在 `knowledge.set_profile`
会一并清 `answer` / `termfix` 缓存。

### 测试

54 → **64** 个：`tests/test_merge_gate.py`(14) / `test_termfix.py`(12) /
`test_topic_stack.py`(10)。

## 0.9.2

**检索：指代型追问的两处修正**（39 条有明确材料的真实提问，同一套 harness 前后对照）

先把上一轮的**负面调研结论**记下来，省得后来者重走：原计划用「问题里没有任何语料词」判
「无实词追问」，实测 50 条人工标注里只命中 1 条、且那条检索本来就是对的；6 条真正漏掉的题
**全都有语料词**（功能/一家/小组/承担）。真正的失效模式是**指代型追问**——有内容词、但全是泛词，
指向对象在上一轮（"还有哪些功能吗""你做的占比估计有多少"）。

- **HR 守卫的动作错了（主因）。** `HR_HINT` 命中后原来是 `return None`（完全放弃加权），
  这一条就吃掉 hit@1。改成**把加权目标换成 HR 材料**（`HR_FILES`）后：
  8 句窗口 hit@1 84.6% → **89.7%**、hit@3 92.3% → **94.9%**、题库并集 94.9% → **97.4%**。
  守卫的**意图**是对的（HR 题别继承项目话题），**动作**错了——该换目标，不是放弃加权。
- **追问的「材料继承」。** 指代型追问与目标材料**零词面交集**，这时任何 boost 都是乘在一个
  0 分的 chunk 上，救不回来。做法：把**上一题命中的那块材料**也检索一遍，与本句检索做 RRF 融合
  （主路权重恒为 1.0，是**融合不是替换**）。门控：本句没有话题词 + ≤40 字 + 不是 HR 题——
  无条件继承实测 hit@1 掉 7.7（会把"自我介绍"这种题也拖到上一题）。
- 上一题的锚点用**记忆**而不是拿 history 反推：`main.py` 只传 `recent[-8:][:-1]` 共 7 句，
  反推出来的窗口比上一题当时看到的少一句，锚点会丢（#26 就是这么丢的）。

**诚实边界**
- 仍有 2 条失败（#38「还有哪些角色呢」、#39「你做的占比估计有多少」）：面试官在 HR 话题后切回项目，
  但这个切换在词面上不可见；39 条样本也谈不上统计意义，没有硬凑。
- `INHERIT_NEED_PREV_ANCHOR=True` 更保守、hit@1 略高，但 hit@3 在 7 句窗口只有 92.3%，
  不像默认值那样两个窗口都稳在 94.9%，所以默认关。
- 参数落在一片平台上（回看 7~14 句、上一题最小长度 0~12 结果完全一致），不是刀尖调参。
- 回滚：`INHERIT_ENABLE = False` 一行退回旧行为；掉分了就把 `INHERIT_WEIGHT` 调小。
- 顺带发现：线上只给 7 句上文，同一套代码在 7 句窗口下的成绩（82.1% / 89.7%）本来就比
  `tools/regress.py` 的 8 句窗口低 2.6 个点——**离线门禁比线上更宽松**，这点值得知道。

**修复**
- `tests/test_repo_hygiene.py` 的两条扫描走的是**文件系统**而不是 git，本地 `_realdata/` 一放录音
  就会误报「仓库里混进了大文件 / 私人路径」。把 `_realdata` 加进跳过名单，
  并加一条断言保证该目录确实在 `.gitignore` 里（守住原来的强度）。

## 0.9.0

首个公开版本，从个人自用版本整理而来。

**核心链路**
- 系统回环音频采集（20ms 一帧，16000Hz 单声道）+ 能量 VAD 判停
- SenseVoiceSmall ONNX int8 本地识别（RTF 0.025~0.03，进程内存约 491MB）
- BM25 + jieba 本地检索（索引约 1s 建好，单次检索 0~1ms）
- DeepSeek `deepseek-flash` 流式快答（首字 281~831ms，答案 150~300 字）
- 端到端：面试官说完 → 第一行答案上屏 约 1.3~1.5 秒

**浮窗（PyQt6）**
- 置顶、防共享（WDA_EXCLUDEFROMCAPTURE）、全局热键（Ctrl+Shift+H/P/D/E）
- 默认「只看核心句」，核心句超过 40 字给黄色警示（别照念）

**健壮性**
- 事件带 qid，连问不串题；首字看门狗；采集循环自愈；端口冲突自动顺延
- 会话落盘 logs/session_*.jsonl，配 tools/session_report.py 出赛后复盘

**已知边界**（如实记录，别当成 bug 修）
- 材料里没写过的专有名词，走「坦诚」话术而不是编造
- ASR 会把专有名词写坏，靠 config/known_terms.md 里的词表兜
- 字级 bigram 检索通道实测为负收益，默认关闭；话题续接失败仍是主要未解问题

## 0.9.1

**体验**
- 浮窗定位改为**跟随鼠标所在屏幕**，并把坐标夹进该屏的可用区。原来只用 `primaryScreen()`：
  双屏时会议软件在副屏、浮窗却出现在主屏（用户以为没启动）；1366x768 这类小屏 + 150% 缩放
  下 `right()-610` 还可能算出屏外坐标。启动时会打印一行
  `[浮窗] 屏幕「...」可用区 1720x1019 @(0,0) 缩放 1.50 → 浮窗 580x660 定位 (1103,40)`，
  定位不对时一眼能看出。
- **手感参数可配**（写进 `config/settings.json`，不改代码）：
  `vad_end_ms` 判停静音时长（默认 600，调到 350~400 实测能早约 200ms 出答案）、
  `core_max_chars` 核心句字数上限（默认 40）、`core_font_px` 核心大字字号（默认 20）。
  UI 侧与后端读同一份配置，不会出现「浮窗一套默认值、后端另一套」。

**文档**
- 修正 README 里「第一次识别要等 21.7 秒」的说法：21.7 秒是**未预热**时的数字，正常启动路径
  已经由 `boot()` 里的 `warmup()` 吃掉，原文容易让人以为每次启动都要等这么久。
- 「话题续接」的现状写清楚了：已有话题粘滞（实测 hit@1 69.2% → 84.6%），
  剩下的缺口是**完全没有实词**的追问（「那这个呢」）。

**验证**
- `compileall` 通过 · `ruff check .` All checks passed · `pytest` 7 passed
- 浮窗自检实测：屏幕「\\.\DISPLAY1」可用区 1707x1019 缩放 1.50 → 定位 (1103,40)（屏幕右上角，右留 24px、上留 40px）
- 参数实测：临时写入 `{"vad_end_ms":380,"core_max_chars":28,"core_font_px":24}` 后，UI 读到 28/24、后端读到 380；删除后回落默认 40/20/600

## 0.9.3

### ⚠️ 事故与修复：题库被误提交到公开仓库（已抹除）

`tools/build_bank.py` 会把材料跑成 **383 条「口语问法 → 可直接照念的答案」**，
落在 `知识库/题库.json`（181 KB）。这个路径**不在 .gitignore 里**，一次 `git add -A`
就把它带进了公开仓库。

- **窗口**：约 4 分钟（推送 → 发现 → `git rm --cached` + `--amend` + 强推）。
- **处理**：已从提交中移除并强推；独立重新克隆验证 —— 远端查不到该文件、
  raw 接口返回 404、全历史无痕。GitHub 侧的对象会在后续 GC 中回收。
- **根因**：**生成物比原始材料更容易被漏掉**。原始材料（录音、`knowledge/`）你知道要藏，
  生成物只被当成「跑出来的中间文件」；可它的内容是材料的逐条改写，敏感度完全一样。
- **加的防线**：`.gitignore` 增加 `知识库/` / `bank.json` / `*题库*.json`；
  卫生测试新增 `test_generated_artifacts_from_private_material_are_ignored`，
  保证这几条规则不会被谁手滑删掉。
回答四个「要不要做」的问题：按公司分包有没有必要、资料多会不会拖慢检索、
面试官问具体接口名/参数名怎么办、别人的面试录音能不能进知识库。**先量了再动手。**

### 新增：资料包 —— 按公司把材料切开（`corpus_profile`）

- `knowledge/_base/` = 通用个人材料；`knowledge/<公司名>/` = 该公司的 JD、公司介绍、面经。
  `corpus_profile` 填公司名，检索就只在这两块里做；**留空 = 老行为**（扫整个 `knowledge/`）。
- 管理命令 `python tools/profiles.py --list | --new | --check | --show`。
- 包里的 `company.md` 既当「公司背景」注入、也参与检索；`config/company.md` 优先级更高。
- 文件名以 `_` 开头的材料不参与检索（放笔记用）。包名打错会打印提示，不静默降级。
- **兼容性**：默认配置下语料加载结果与改动前**逐块完全相同**（真实材料 9 份 / 338 块，
  chunk 序列逐一比对一致）。

### 负面结果：为了补最后 2 条 hit@3，六个改法全被实测否定

39 条 GOLD 上还有 4 条 hit@1 未中，其中 #38/#39 连 top-3 都没进。逐个试了一遍：

| 改法 | 结果 |
|---|---|
| 用检索分数当置信度 | 不行：分数只是排名的函数（排第 1 恒为 0.9836），不含独立信息 |
| 用「查询词被覆盖多少（IDF 加权）」当置信度 | **方向是反的**：命中中位 0.181，未命中中位 0.276 |
| `HR_HINT` 去掉 `角色`/`占比` | 零变化 —— 继承门用同一个正则，拿掉后门跟着开，又把上一题的材料拖回来 |
| 查询窗口剔除 HR 句 | 零变化 —— HR 词本来就在当前句里 |
| HR 加权 3.0 → 1.5 | 零变化；→ 1.0 更差（82.1/87.2） |
| sticky 判定用更严 HR 正则 | **更差**：87.2/92.3 |
| 把「角色/占比」补进项目的话题词表 | **零变化**（只加「架构/功能」这类泛词才 +2.6，但修的是另一条题，泛词会误触发） |

结论：#38/#39 的「角色 / 占比」在 HR 材料里更密集，同样一句在 HR 场景下也是合理提问 ——
**要分开得靠语义，词表/权重分不开**；#4 是 ASR 听错（"在暂时吗？"），检索层救不了转写错误。
另有一个「来源多样性」实验因 RRF 的池子变化导致两臂不可比，**未采纳，默认行为不变**。
完整数据见 [性能实测](docs/性能实测.md) 的「检索的真实边界」。

顺带纠正上一版的一个说法：0.9.2 的笔记称「不加 HR 守卫会让 hit@3 掉 3 条」，
今天逐条量出来——守卫整体是正收益（+7.6 hit@1），但 `占比` 归到 HR 是它的**代价**，不是收益。
### 新增：私有词表搬到 config/ —— 一份代码 + 一份你的配置

以前语料路径、领域词表（`DOMAIN_WORDS`）、话题词表（`TOPIC_TERMS`）是**硬编码在
`server/knowledge.py`** 里的。公开仓库那份只能放示例占位词，于是有两个真相：私人副本
能跑但对不上上游，仓库版对得上上游但检索是空的 —— 每改一次就得同步两份。

- `config/domain_words.txt`、`config/topic_terms.json`（都在 `.gitignore` 里）现在会覆盖
  代码里的示例；文件不存在就继续用示例，不会把内置词表清空。
- 迁移工具 `python tools/migrate_private.py --from <旧副本>/server`（默认只报告，
  `--write` 才落盘；用 AST 读常量，不 import 对方代码）。示例见同目录 `.example` 文件。
- **验证**：纯仓库代码 + `config/` 配置（不叠加任何旧副本代码）跑 39 条 GOLD，
  hit@1 **89.7%** / hit@3 **94.9%**，与旧副本逐位一致 → 可以零损失切换。

另修：两条仓库卫生检查走的是**文件系统**、看不到 `.gitignore`，于是被忽略的
`config/settings.json`（含本机材料路径）被误报成「要提交的私人数据」。历史上已栽过两次
（先 `_realdata/`，再 `config/`）。现在统一改成问 **git 自己的忽略规则**
（`git check-ignore --stdin`），而不是继续往手写跳过名单里加条目；
并自查过「真泄露文件仍然会被抓到」。

### 新增：标识符压平 —— 面试官问具体接口名/参数名/函数名时能对上了

材料里写 `max_len`，面试官说的话转写成 `maxlen`（ASR 听不见下划线），
原来两边分词成 `{max,len}` vs `{maxlen}`，**一个词都对不上**。现在给所有带分隔符的
ASCII 标识符额外产出压平形式（`max_len`→`maxlen`、`adapter_config.json`→`adapterconfigjson`、
`utf-8`→`utf8`），只加不删，原有分词一个不动。

实测（742 个「只属于一个文件」的真实标识符，问句为「<标识符> 是怎么定的？」）：

| | hit@1 | hit@3 |
|---|---|---|
| 压平前 / 原样问 `max_len` | 95.0% | 98.3% |
| 压平前 / **ASR 压平问 `maxlen`** | **0.0%** | 35.0% |
| 压平后 / 原样问 | 98.3% | 100.0% |
| 压平后 / **ASR 压平问** | **73.3%** | 85.0% |

39 条 GOLD 无回归（89.7% / 94.9% 不变）。驼峰（`LangGraph`）jieba 本来就整块留着，不受影响、
也不额外切出 `lang`/`graph` 这种泛词。

**仍未覆盖**：ASR 把驼峰读成两个词（`Lang Graph`）时，材料里的 `langgraph` 还是对不上。

### 实测：知识库规模对检索的影响（回答「资料多了会不会变差」）

39 条 GOLD、8 句窗口、`DEFAULT_TOPK=3`。往 338 块的知识库里按比例掺入外来文本：

| 掺入 +25%（≈84 块） | hit@1 | hit@3 | 噪声块占 top3 |
|---|---|---|---|
| 不掺 | 89.7% | 94.9% | 0% |
| 异题材（技术文档类） | 87.2%（−2.5） | 94.9%（0） | 4.3% |
| 别人的面试录音（37 场） | 84.6%（−5.1） | 89.7%（−5.2） | 12.8% |
| **本人**的面试录音（4 场） | **41.0%（−48.7）** | 87.2%（−7.7） | 41.0% |

- **速度不是问题**：338 块 1.8ms/题，1014 块 4.1ms/题，都是毫秒级。不用为性能分包。
- **纯规模几乎不伤准确率**：掺 2 倍异题材文档 hit@1 只掉 2.5 → 参考资料可以放心多放。
- **同题材竞争才是杀手**：噪声块占 top3 从 4% 飙到 41%，直接抢排名。
- 口径提醒：每档只跑一次、样本 39 条，对话体与文档体分布不同；上面「异题材影响小」最稳，
  本人录音的绝对幅度别当精确值。

## 下一版计划

- 计划：macOS / Linux 端调研（当前仅 Windows）；双屏多显示器场景欢迎反馈