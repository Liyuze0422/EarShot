# DeepSeek API Key —— EarShot 快答线用。
#
# 用法：把本文件复制成同目录的 api_key.txt，把你的密钥单独写一行进去。
#   1. 一行就够，只放密钥本身：不要引号、不要分号、不要在前后加空格。
#   2. 以 # 开头的行会被忽略，所以下面那行示例保持注释状态即可。
#   3. 也可以跳过文件，直接用环境变量：TP_API_KEY=sk-xxxx（临时用最方便）。
#   4. 密钥申请：https://platform.deepseek.com  →  API keys
#
# config/api_key.txt 已在 .gitignore 里，不会被提交到仓库。
#
# sk-xxxxxxxxxxxx
