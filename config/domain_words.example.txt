# 领域词表：在你材料里会被 jieba 切碎、但面试官一问就是关键词的专有词。一行一个。
#
# 用法：复制成同目录的 domain_words.txt 再改（那份已被 .gitignore 挡住，不会进 GitHub）。
# 为什么需要：jieba 默认词典会把"工作流"切成"工作/流"、"有向图工作流"切成
# "有向图/工作/流"，术语对不上，检索就丢分。
# 换项目时把下面整段替换成你自己的词 —— 产品名、框架名、业务黑话都算。
#
# 也可以直接改 server/knowledge.py 里的 DOMAIN_WORDS，但那样改的是**公开仓库的代码**，
# 下次升级就冲突了。放这里更好。
#
# 从旧副本搬过来：python tools/migrate_private.py --from <旧副本>/server --write

# ---- 下面是示例，请替换 ----
推荐系统
双塔
粗排
精排
冷启动
协同过滤
特征工程
排序模型
离线评估
线上指标
点击率
转化率
