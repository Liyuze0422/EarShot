# 目标公司背景（会前填，每题都会带进上下文）

> 这份是随仓库发布的**模板**（config/company.example.md）。
> 用法：把它复制成同目录的 config/company.md 再改 —— company.md 已被 .gitignore 忽略，
> 里面有公司名和 JD，属于你本机的东西，不会被提交。
> 程序找不到 company.md 时会退回读这份模板，所以不复制也能跑，只是没有"对方业务"可结合。
> 下面以「（例」开头的行是占位示例，程序会整行跳过；不删也不影响答案。

## 公司名 / 业务

（例：某某科技，做电商推荐与搜索，主要客户是垂直电商）

## 岗位 JD 关键要求

（例：Agent Engineer，要求大模型应用落地、检索与排序、RAG、有 ToB 交付经验）

## 技术栈线索

（例：Python、LangGraph、Qwen、私有化部署、K8s）

## 面试官是谁 / 什么轮次

（例：技术二面，面试官是 Agent 组 leader）

## 我特别想强调的匹配点

（例：我做过推荐系统的召回与粗排，直接对口；我有 ToB 私有化交付经验）
