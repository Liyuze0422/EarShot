# -*- coding: utf-8 -*-
"""问题分类：决定用哪种回答策略。规则法，0 延迟（<1ms）。

分类准则不是"语言学分类"，而是"这题该用哪套答案策略"：
  experience  答案必须由我的真实材料支撑（我的项目 / 我的做法 / 我的履历 / 我的团队角色）
  design      材料里没有，要现场搭思路，可能结合对方公司
  behavior    冲突 / 失败 / 缺点 这类要讲故事的题
  motivation  动机、离职、规划
  reverse     把提问权交给我
  skip        不是提问（闲聊、设备调试、面试官自述）
  unknown     兜底

为什么这么宽：用 2026-08-31 那场真实一面（50 段面试官发言）做回归测试发现，
面试官的实际提问极其口语化——"那个推荐系统是你自己搭的吗""数据量有多大呢""双塔是吧"——
旧规则只认"你在 / 怎么做的 / 为什么用"这类书面词，真实题命中率只有 2.3%。
新规则把"是不是 / 对吗 / 哪些 / 什么作用 / 怎么来"这一层也算进来，
并且做了一条兜底判断：只要是真提问、又没命中其它策略，就按经历线走
（真实面试 44 条提问里 39 条是项目深挖，经历线对材料最敏感、最不容易编）。
"""
import re

# ── 1) 不是提问：设备调试 / 寒暄 / 面试官自述 / 跑题的设备闲聊 ──────────────
SKIP = [
    r'听得到吗', r'听得到', r'能听到', r'听得清', r'听得见',
    r'声音(大|小|放大|清楚)', r'视频(出来|开了?|卡|没)',
    # ⚠️ 这里原来还有裸的 r'麦克风' / r'摄像头'。2026-09-14 实机演练（真实面试录音
    # 走完整采集链路）证明它会把**真问题**误杀：HR 问"从麦克风开始到机器人做出
    # 动作，你们中间会经过哪些环节呢"，整句因为含"麦克风"三个字被判设备调试，
    # 一道核心问题直接漏放。设备调试话本来就不带疑问形式，末尾 QUESTION_FORM
    # 兜底拦得住（"稍等我看一下麦克风"→无问句→skip），所以裸设备词不需要。
    r'咋没有', r'怎么没有(视频|声音|画面)', r'稍等', r'等我一下', r'我先打开',
    r'^我了解了', r'我这边(没什么|没有问题)', r'^(行|好)[，,]\s*我这边', r'你继续',
    r'我为什么这么问', r'我这么问(是)?因为', r'我是本(次|场)的面试官',
    r'你(的)?(手机|电脑|网络)(内存|配置|型号|怎么样|什么牌子)',
    r'^(喂|你好|在吗|在不在|嗯|哦|好的|行)[?？。！!，,]*$',
    # 2026-09-20 实测补：设备确认被当成面试题答了 4 次（「卡了吗?」「能看到吗?」
    # 「你说?」）。它们不是问题，答了只会往屏幕上塞一段无关内容。
    r'能看到(吗|吧|没)|(卡|掉线|断)(了|掉)?(吗|没)|^你说[?？。！!]*$',
]
# ── 2) 开放式设计题：假设句 / 从零搭 / 结合对方公司 ────────────────────────
DESIGN = [
    r'如果(让|给|是|要|我们|需要|你)', r'假如', r'假设', r'要是(让|你)',
    r'你会(怎么|如何|选|设计|做|落地)', r'你打算(怎么|如何)',
    r'你(会)?怎么(去)?(设计|搭建|建|构建|打造|做技术选型|落地)',
    r'如何设计', r'怎么设计', r'怎么(去)?(搭建|搭|构建|建设|负责)',
    r'结合(我们|贵|你们)', r'(给|帮)我们(设计|提|想|做)', r'提个方案',
    r'有什么思路', r'你的思路', r'你怎么看', r'从零',
]
# ── 3) 行为面试题：要讲故事 ───────────────────────────────────────────────
BEHAVIOR = [
    r'冲突', r'缺点', r'不足(是|在哪)', r'失败', r'遗憾', r'压力', r'挫折',
    r'最难(的)?(一次|经历|问题|地方)', r'讲(一次|一个|个|讲).{0,8}(经历|事|例子)',
    r'团队协作', r'协作', r'被.{0,4}质疑', r'犯过.{0,4}错', r'怎么处理.{0,6}(关系|分歧)',
    r'沟通过.{0,6}(困难|问题)',
]
# ── 4) 动机意向题 ────────────────────────────────────────────────────────
MOTIVATION = [
    r'为什么(选|来|想|要|会).{0,6}(我们|公司|贵司|这里|一家)', r'职业规划',
    r'未来.{0,6}(打算|规划|计划)', r'(三|3|五|5)年.{0,6}(规划|计划|打算)',
    r'期望.{0,6}(薪资|待遇)', r'为什么离职', r'为什么(要)?(换|离开)',
    r'为什么.{0,6}(刚)?毕业.{0,6}(换|跳)', r'为什么.{0,6}(换|跳|离开).{0,6}(公司|一家)',
]
# ── 5) 反问机会 ──────────────────────────────────────────────────────────
REVERSE = [
    r'你(还)?有什么.{0,6}(想问|要问|问题|提问|想了解|了解)',
    # 2026-09-20 实测漏放：「，看看你这边有什么想了解的吧。」——中间插了「这边」
    # 就匹配不上了，而那是面试官在把提问权交过来，答不上很亏。
    r'有什么想(了解|问)|你这边有什么',
    r'有(什么|没有).{0,6}问题.{0,6}(问|了解)', r'反问', r'你想(要)?了解(什么|哪些)',
]
# ── 6) 真提问兜底：有疑问形式就当"在问我" ─────────────────────────────────
# ⚠️ 这条兜底曾经是"只要出现 什么/怎么/为什么 就当在问我"。
# 代价实测（953 条真实环境音 + 44 条人工标注的真实面试提问）：
#   · 它一个人贡献了 95% 的**误放行**（LOL 解说里的"什么都没交"、"他怎么先落位"）
#   · 同时也贡献了 89% 的**正确召回** —— 所以不能一删了事，得收紧而不是关掉
# 收紧后：误放行 25.4% → 14.1%，召回**仍然 100%**。
# 收的方向是「问的形式」而不是「有疑问词」：疑问词要挨着动词，或者前面有"你"。
#   · 具体没有问句词、但确实在问你的 → 靠 DESIGN/BEHAVIOR/MOTIVATION/REVERSE 那几条管
QUESTION_FORM = re.compile(
    r'[?？]|'
    # ⚠️ 2026-09-20：这里原来是 (吗|嘛)。真实面试数据（一场 36 题的逐条复盘）证明
    # 「嘛」是语气词、不是疑问：面试官介绍公司时张口就是「…正式发布的嘛」「半年到
    # 一年嘛都是围绕字节赚的」，6 条长陈述被它单独放行、白答一遍。删掉「嘛」；
    # 「吗」保留（「是吗」「理解吗」确实是问句）。
    r'吗(?![^\s]{0,2}的)|'                                      # "…吗"结尾，排除"…吗的"
    r'你.{0,8}(怎么|如何|为什么|为啥|做过|用过|有没有|是不是|哪一?[个些种层块条台])|'
    r'(介绍|讲讲|说说|举例|聊一聊|描述)(一下|下|你)?|'
    # 「啥」是「什么」的口语形式，真实提问里高频（“你说一说这一年半的感受是啥样的”）。
    # 原来没有它 —— 那条真问题当时是靠误伤的「嘛」凑巧接住的，所以这两处必须同改，
    # 只删「嘛」不补「啥」会掉召回。
    r'啥(样|的|时候)|干啥|为啥|'
    r'(什么|怎么|如何|为什么|为啥)(做|实现|理解|设计|处理|解决|保证|考虑)|'
    r'哪一?[个些种层块条台]|多少|几(个|种|台|层|条)')

# 长句里的疑问形式多半是定语或引述，不是提问（同一场实测：「你会快速知道中国**哪些**
# 行业…」「**让你**去看到这些业务是**怎么**回事」）。面试官真提问时，问句总落在最后，
# 所以判据用**末尾窗口**而不是整句：够长、且末尾没有疑问形式 → 按陈述过滤。
LONG_STATEMENT = 40   # 超过这个长度才启用（短句一律照旧，避免误伤）
TAIL_WINDOW = 20      # 只看最后这几个字 —— 要够长才装得下"你为什么当时上了这个学校"这种倒装问句

# 末尾窗口**只认强疑问形式**，这是和整句判断的关键区别：
#   · 「哪一些」「几个」在陈述里常作定语（"你会快速知道中国哪些行业…"），单独出现不能
#     当成"在问我" —— 实测就是它们把一条公司介绍放行了；
#   · 而"你为什么当时上了这个学校和这个专业"是真提问，只是疑问词倒装在句中。
# 窗口放宽到 20 字后，用这张强表把两者分开：既救回倒装问句，又不把定语放回来。
_TAIL_Q = re.compile(
    r'[?？]|吗(?![^\s]{0,2}的)|啥(样|的|时候)|干啥|为啥|'
    # 末尾这一支**不要**「你…怎么/如何」：「让你去看到这些业务是**怎么**回事」是定语，
    # 实测放行了一条公司介绍。真问句「你会怎么做」一定带问号、或落在句中，不靠这里兜。
    r'你.{0,8}(为什么|为啥|做过|用过|有没有|是不是)|'
    r'(什么|怎么|如何|为什么|为啥)(做|实现|理解|设计|处理|解决|保证|考虑)|'
    r'(介绍|讲讲|说说|举例|聊一聊|描述)(一下|下|你)?')

_SKIP = [re.compile(p) for p in SKIP]
_RULES = [
    ('design', [re.compile(p) for p in DESIGN]),
    ('behavior', [re.compile(p) for p in BEHAVIOR]),
    ('motivation', [re.compile(p) for p in MOTIVATION]),
    ('reverse', [re.compile(p) for p in REVERSE]),
]

LABEL = {
    'design': '开放式设计题',
    'experience': '经历深挖题',
    'behavior': '行为面试题',
    'motivation': '动机意向题',
    'reverse': '反问机会',
    'skip': '非提问(过滤)',
    'gap': '没准备过的名词',
    'unknown': '通用题',
}


def is_skip(q):
    """不是提问 → 不该浪费一次大模型调用。"""
    s = (q or '').strip()
    if len(s) < 2:
        return True
    for p in _SKIP:
        if p.search(s):
            return True
    # 命中明确规则的题（设计/行为/动机/反问）一定是提问，不看兜底。
    # 兜底和明确规则是**或**的关系，不是先后关系 —— 踩过：
    # 收紧兜底后「说一次你做得比较失败的项目，你从里面学到了什么」被判 skip，
    # 因为它不含任何疑问词，可它明明命中 BEHAVIOR 的「失败」。分类题丢一条比多答一条亏得多。
    for _, pats in _RULES:          # _RULES 里是编译好的；DESIGN 那几张是原始字符串表
        for p in pats:
            if p.search(s):
                return False
    # 既没问号也没任何疑问形式 → 面试官在下结论/自言自语
    if not QUESTION_FORM.search(s):
        return True
    # 够长、而且末尾窗口里没有疑问形式 → 判为陈述（疑问形式只出现在句中，多半是定语）
    if len(s) >= LONG_STATEMENT and not _TAIL_Q.search(s[-TAIL_WINDOW:]):
        return True
    return False


def classify(q):
    if is_skip(q):
        return 'skip'
    for name, pats in _RULES:
        for p in pats:
            if p.search(q):
                return name
    if QUESTION_FORM.search(q):
        return 'experience'
    return 'unknown'
