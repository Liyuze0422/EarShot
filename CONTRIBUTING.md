# 贡献指南

欢迎提交 Issue 与 PR。这个项目的定位很窄 —— **一场真实面试的提词器**，所以：

## 什么样的改动容易被接受

- 端到端延迟还能再降（现在是说完 1.3~1.5 秒上屏）
- 识别 / 检索命中率变好，并且**附上可复现的评测脚本与前后对比数字**
- 采集链路更稳（插拔耳机、切默认设备、静音、系统更新后失效）
- 浮窗在真实会议软件里更不容易出问题（共享、全屏、多屏、DPI 缩放）
- 文档里写错的地方（尤其是「诚实边界」那一节）

## 什么样的改动会被拒

- 把「猜测」包装成「能力」：本项目宁可说「材料里没有」，也不让模型编
- 引入体积巨大的依赖，或把模型权重提交进仓库（请用 tools/download_model.py）
- 提交任何个人的面试材料、密钥、会话日志（CI 里的 tests/test_repo_hygiene.py 会拦住）

## 提 PR 前请自查

```powershell
python -m compileall -q server ui tools
ruff check .
pytest -q
```

如果改动涉及延迟或准确率，请在 PR 描述里贴上**改动前后的实测数字**与跑测的命令。